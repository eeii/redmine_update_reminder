<%=@user.name%>,

<%=  Setting.plugin_redmine_update_reminder['header'] %>

<% @issues_with_updated_since.each_with_index do |issue_with_updated_since, i| -%>
  <%issue, updated_since = issue_with_updated_since%>
    <% i+1%>. <%= link_to_issue(issue, project: true, only_path: false) %>
    已经 *<%=time_ago_in_words(updated_since)%>* 没有更新进展了。
<% end -%>
</ul>

需要立即关注。

<br/>
<%=  Setting.plugin_redmine_update_reminder['footer'] %>
