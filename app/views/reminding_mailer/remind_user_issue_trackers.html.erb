<%=@user.name%>,
<br/><br/>
<%=  Setting.plugin_redmine_update_reminder['header'] %>

<ul>
<% @issues_with_updated_since.each do |issue_with_updated_since| -%>
  <%issue, updated_since = issue_with_updated_since%>
  <li>
    <%= link_to_issue(issue, project: true, only_path: false) %>
    已经 <strong><%=time_ago_in_words(updated_since)%></strong> 没有更新进展了。
  </li>
<% end -%>
</ul>

需要立即关注。

<br/>
<%=  Setting.plugin_redmine_update_reminder['footer'] %>
